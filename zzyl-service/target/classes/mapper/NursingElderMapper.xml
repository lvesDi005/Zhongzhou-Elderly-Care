<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzyl.mapper.NursingElderMapper">

    <select id="selectUserByElderId" resultType="com.zzyl.vo.UserVo">
        SELECT su.id AS uid, su.real_name AS real_name
        FROM elder_nursing en
                 LEFT JOIN sys_user su ON en.nursing_plan_id = su.id
        WHERE en.elder_id = #{elderId}
    </select>

</mapper>
