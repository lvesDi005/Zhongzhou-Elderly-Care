<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzyl.mapper.NursingLevelMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into nursing_level(name, lplan_id, fee, status, description, create_time, create_by, update_by, remark)
        values (#{name}, #{lplanId}, #{fee}, #{status}, #{description}, #{createTime}, #{createBy}, #{updateBy}, #{remark})
    </insert>
    <update id="update">
        update nursing_level
        set name        = #{name},
            lplan_id    = #{lplanId},
            fee         = #{fee},
            status      = #{status},
            description = #{description},
            update_by   = #{updateBy},
            update_time = #{updateTime},
            remark      = #{remark}
        where id = #{id}
    </update>
    <select id="selectByPage" resultType="com.zzyl.entity.NursingLevel">
        select * from nursing_level
        <where>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by id
    </select>
</mapper>
